from setuptools import setup, find_packages

setup(name='simim',
      version='2.0',
      description='Code for simulating intensity mapping experiments',
      url='https://github.com/rpkeenan/simim',
      author='R P Keenan',
      packages=find_packages())
